name: CI Test

on:
  workflow_dispatch:

  push:
    branches:
      - main
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test workflow
        run: echo "GitHub Actions is working"